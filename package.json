{
  "name": "sbgck-fsme-test",
  "version": "1.0.0",
  "description": "",
  "main": "dist/main.js",
  "types": "./dist/main.d.ts",
  "scripts": {
    "build": "tsc",
    "start": "ts-node src/game.ts",
    "startMain": "ts-node src/main.ts",
    "prepublishOnly": "tsc",
    "prepare": "tsc",
    "po": "rm -f uk.po* && ttag init uk uk.po && ttag update uk.po src/game.ts && ttag po2json uk.po > uk.po.json && cat uk.po.json | jq '.translations.\"\" | keys | .[]' -r | sed '/^$/d' >texts.txt && rm -f uk.po*"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "javascript-state-machine": "^3.1.0",
    "ttag": "^1.7.24"
  },
  "devDependencies": {
    "@types/node": "^14.14.31",
    "ts-node": "^9.1.1",
    "ttag-cli": "^1.9.2",
    "typescript": "^4.2.2"
  }
}
